# ── Microsoft Agent Framework ────────────────────────────────────────────
# SDK in preview — pin exact versions to avoid breaking renames
agent-framework-azure-ai==1.0.0rc3
agent-framework-core==1.0.0rc3
azure-ai-agentserver-agentframework==1.0.0b17
azure-ai-agentserver-core==2.0.0b3

# ── FastAPI / ASGI server ─────────────────────────────────────────────────
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
httpx>=0.28.1
python-multipart>=0.0.26
python-jose[cryptography]>=3.5.0

# ── Azure identity + clients ─────────────────────────────────────────────
azure-identity>=1.25.3
azure-cosmos>=4.15.0
# Pinned: agent-framework-core 1.0.0rc3 requires exactly 2.0.0b4
azure-ai-projects==2.0.0b4

# ── Utilities ────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
pyyaml>=6.0.3
pypdf>=4.0.0
python-docx>=1.1.0
requests>=2.33.1
